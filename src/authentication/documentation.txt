* Registrar Usuario
mutation{
	createUser(user: {
		email: "loquesea@loquesea.loquesea"
		name: "usernuevo"
		nickname: "usernuevo"
		password: "123456"
		password_confirmation: "123456"
	}) {
	id
	email
	name
	nickname
	token
	type
	client
	uid
	}
}

* Iniciar Sesion
Usuario para pruebas ercruzr@unal.edu.co

mutation{
  createSession(session: {
	email: "ercruzr@unal.edu.co"
	password: "123456"
	}) {
	id
	email
	name
	nickname
	token
	type
	client
    uid
	}
}

* Consultar el id del Usuario
query{
    userById(idUser: 1){
	id
    name
    nickname
    email
    }
}

* Consultar todos los usuarios
query{
	userAll{
    id
    name
    nickname
    email
	}
}

* Validación del token del usuario (token y client cambian)
query{
	validateToken(headers: {
	token: "fP5AaYSmWTxkPeXC4JyAug"
	uid: "ercruzr@unal.edu.co"
	client: "oMamwjXeVnHgQ-0BbeLyYw"
	}) {
	id
	email
	name
	nickname
	token
	type
	client
	}
}

* ldap
1. Se debe iniciar el servidor ldap en http://146.148.107.218:8085
   y crear un usuario de dominio unal (en la parte de common name
   es donde va el usuario con correo unal)
2. Ir a http://146.148.107.218/graphiql y crear un usuario
   (igual que en el anterior), con la mutacion createUser
3. En graphiql hacer lo siguiente:

mutation{
  createSessionLDAP(session: {
	email: "prueba3@unal.edu.co"
	password: "123d456"
	}) {
	email
    answer
	}
}

donde answer da la respuesta si esta o no en la base de datos